# 26-01-26 
Alrighty, this time I want to play with the length function.
```glsl
void main(){
    vec2 uv = gl_FragCoord.xy/u_resolution.xy;

	vec3 color = vec3(0.0);
    uv -= 0.5;
    vec2 st2 = st;
    vec3 color = vec3(0.);
    float dist = length(abs(st)-0.492);
    float dist2 = length(abs(st2)-0.492);
    
    color.r = smoothstep(0.41,0.5,cos(dist+sin(u_time)*0.5-0.5));
    color.g = smoothstep(tan(dist+sin(u_time+400.)*0.5-0.5),0.15,.5);
    color.b = smoothstep(0.41,cos(dist+cos(200.+u_time)),0.5);
	gl_FragColor = vec4(color,1.0);
}
```