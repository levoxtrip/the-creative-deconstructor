# 25-12-30
Ok, on my day two. Let's see what I like doing today. 
```glsl
void main(){

	
	uv += vec2(sin(u_time),-cos(u_time));

	vec3 color = vec3(sin(u_time)*uv.x, uv.y,tan(u_time)*uv.x);
	gl_FragColor = vec4(color,1.0);
}
```