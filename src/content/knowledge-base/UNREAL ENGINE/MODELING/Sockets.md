---
title: Sockets
tag: Unreal
---

# Sockets
Sockets allow to specify locations on a mesh that we can access later on. Go into the mesh menu and look for `Socket Manager` where you can add a socket with the `+`. Make sure that the socket is pointing in the right 
direction. To access the information inside a blueprint you can use `Get Socket Location` or `Get Socket Rotation`.


