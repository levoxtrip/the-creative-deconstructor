---
title: Camera Lag On Spring Arm
tag: Unreal
---

# Camera Lag On Spring Arm
The `Spring Arm Component` has a parameter that allows to `Enable Camera Lag`. Under `Camera Lag Speed` you can set how fast the camera catches up with the mesh. The lower the slower the value the slower camera. Makes camera movement feel smoother.