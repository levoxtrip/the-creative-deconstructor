---
title: Only One Sample Of CHOPData
comments: true
tags:
 - TD/CHOP
 - TouchDesigner
---
![Switch Between CHOPS](/img/TD/OnlyOneSampleOfCHOPData.png)

# Only One Sample Of CHOPData
To get only one sample of a chop data we can use `trimCHOP`. Set `Unit Value` to `Absolute` and give the same index to `Start` and `End`.
This is useful if you want a special index of the channel data.