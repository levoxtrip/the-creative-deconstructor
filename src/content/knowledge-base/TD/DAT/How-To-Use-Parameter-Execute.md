---
title: How To Use Parameter Execute
tag: TD
---

# How To Use Parameter Execute
In a `ParameterExecuteDAT` we can assign an operator and then access the operators parameter by adding the parameter name in the `Parameters` field.
In the code we then can check for the parameter name `if par.name == 'seed': do this`

![How To Use Parameter Execute Img](/img/TD/HowToUseParameterExecute.png)